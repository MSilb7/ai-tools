# Compounding — 2026-07-22 11:00 UTC

Source: the 2026-07-19 weekly hygiene sweep (PR #26). Its one [OBVIOUS-FIX] (curly-apostrophe
normalization in two `agents/openai.yaml` sidecars) merged with the PR; these are its three
[PROPOSE] findings, filed here so they stay triageable instead of vanishing into a merged PR body.
Full analysis: PR #26's description.

### [C-HYG1] Collapse the duplicated durable-home routing taxonomy to one canonical home
- **Severity:** low
- **Effort:** low-medium
- **Pickup:** active-agent
- **Ready:** no (needs a framing decision — see background)
- **Files:** `skills/compounding-curate/SKILL.md`, `skills/capture-learning/SKILL.md`
- **Status:** OPEN

**Background:** both skills independently define the same ~9-item "which durable home does this
content belong in" taxonomy (PRD / decision record / technical design / compounding queue / runbook /
build log / shared instructions / provider adapter / skill) in different words. `capture-learning`'s
whole purpose is that routing decision, so it reads as the natural canonical home; `compounding-curate`
could point at it. Not mechanical: the collapse must preserve `compounding-curate`'s framing ("assign
each *existing* block a home") vs capture-learning's ("route a *new* discovery") without losing nuance.

**Goal:** one canonical routing taxonomy + a pointer, per the repo's own duplication rule.

**Acceptance criteria:**
- [ ] The taxonomy lives in exactly one skill; the other references it with its own one-line framing
- [ ] `node scripts/validate-skills.mjs` passes; neither skill loses a routing destination in the diff

### [C-HYG2] `IMPLEMENTATION-DRIFT` tag means two different things across reconcile skills
- **Severity:** low-medium (cross-skill vocabulary collision)
- **Effort:** medium (naming decision, then mechanical rename)
- **Pickup:** active-agent after operator naming call
- **Ready:** no (needs the naming decision)
- **Files:** `skills/prd-reconcile/SKILL.md`, `skills/maintain-technical-design/SKILL.md`
- **Status:** OPEN

**Background:** `prd-reconcile` defines the tag as "behavior contradicts a still-valid *product*
rule"; `maintain-technical-design` defines the same tag as "reality contradicts a still-valid
*technical design*." The two skills hand work to each other, so a pass spanning both cannot tell
which domain a bare tag refers to. Hygiene suggested e.g. `PRODUCT-BEHAVIOR-DRIFT` in prd-reconcile,
keeping `IMPLEMENTATION-DRIFT` for technical design.

**Goal:** each divergence tag names exactly one domain; cross-references updated.

**Acceptance criteria:**
- [ ] Operator (or a recorded decision) picks the rename direction
- [ ] Both skills + any file referencing the old tag updated in one PR; validator green

### [C-HYG3] Decide whether `repository-hygiene` names `codebase-review` as a delegate
- **Severity:** low
- **Effort:** low
- **Pickup:** active-agent
- **Ready:** no (may be a deliberate omission — decide, don't assume)
- **Files:** `skills/repository-hygiene/SKILL.md`
- **Status:** OPEN

**Background:** hygiene's scan dimensions include "dead code, duplication, simplification" and the
skill names `compounding-curate` / `prd-reconcile` / `maintain-technical-design` as delegates for the
*other* dimensions — but never `codebase-review`, whose purpose is exactly that dimension. Possibly
deliberate (hygiene stays lightweight vs codebase-review's heavier audit). Low confidence per the
sweep itself.

**Goal:** an explicit sentence either delegating to `codebase-review` or stating why hygiene keeps
that dimension in-house — no silent ambiguity.

**Acceptance criteria:**
- [ ] One-line resolution lands in `repository-hygiene/SKILL.md` (delegate or deliberate-omission
      note); validator green
